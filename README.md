# my-second-project's Commands
mkdr my-second-project
git init
echo echo "# my-second-project's Commands" >> README.md
echo Hello World > index.html
git status
git add .
git rm --cached README.md
git rm --cached index.html
